# Example environment variables for local development
# Copy this to .env and edit values before running the server
# PostgreSQL (optional - defaults use SQLite if unset)
PGDATABASE=civic_reporting
PGUSER=postgres
PGPASSWORD=your_password
PGHOST=localhost
PGPORT=5432
# Django
DJANGO_SECRET_KEY=e$@300bgna0e-hu_ks*e!5680)85n125g@5ku59y9()hfbfc@1
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_MEDIA_URL=/media/
DJANGO_MEDIA_ROOT=./media

# CLIP model (local path to openai/clip-vit-base-patch32 - do NOT download)
# Example if saved to project: backend/clip_model
# Or Hugging Face cache: C:\Users\You\.cache\huggingface\hub\models--openai--clip-vit-base-patch32\snapshots\<id>
CLIP_MODEL_PATH=./clip_model


# Optional: Hugging Face cache home (used by clip_service fallback)
#HF_HOME=~/.cache/huggingface
EMAIL_HOST_USER= your email
EMAIL_HOST_PASSWORD= yourapp password